export type RLParams = { alphaPlus:number; alphaMinus:number; beta:number; kappa:number }
export type StateDials = { DA_t:number; DA_phi:number; DTD:number; effortW:number; gatingNoise:number }
export type Tip = { id:string; title:string; body:string; reason:string; confidence:number }
